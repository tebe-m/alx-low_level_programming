#include "main.h"
#include <string.h>

/**
 *_strlen - returns the length of the string
 *
 *@s: the input string
 *Return: len
 */
int _strlen(char *s)
{
	int len = 0;

	len = strlen(s);
	return (len);
}
