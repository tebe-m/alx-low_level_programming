#include <stdio.h>

/**
*main - prints all single digit numbers of base 10 starting from 0
*
*Return: Always 0 (success/correct)
*/
int main(void)
{
	int i;

	for (i = 0 ; i < 10; i++)
	{
		printf("%i\n", i);
	}

	return (0);
}
