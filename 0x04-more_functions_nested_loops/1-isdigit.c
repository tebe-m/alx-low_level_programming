#include "main.h"

/**
 *_isdigit -  checks for a digit (0 through 9).
 *
 *@c: number to be chacked
 *Return: 10 if true 0 if false
 */
int _isdigit(int c)
{
	if ((c >= 48)&& (C <= 57))
	{
		return (1);
	}
	return (0);
}
